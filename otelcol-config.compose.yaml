receivers:
  prometheus:
    config:
      scrape_configs:
        - job_name: 'my_job'
          scrape_interval: 5s
          static_configs:
            - targets: ['oteljob:9464']
        - job_name: 'observer_job'
          scrape_interval: 5s
          honor_labels: true
          static_configs:
            - targets: ['observer:9464']

exporters:
  debug:
    verbosity: detailed
  otlp_http:
    metrics_endpoint: http://prometheus:9090/api/v1/otlp/v1/metrics
    tls:
      insecure: true
  prometheus_remote_write:
    endpoint: http://prometheus:9090/api/v1/write
    tls:
      insecure: true
    external_labels:
      path: remote_write
    translation_strategy: ${env:OTELCOL_RW_TRANSLATION_STRATEGY}
    protobuf_message: ${env:OTELCOL_RW_PROTOBUF_MESSAGE}

service:
  pipelines:
    metrics:
      receivers: [prometheus]
      exporters: [debug, otlp_http, prometheus_remote_write]
