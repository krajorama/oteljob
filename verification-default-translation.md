# Verification: One Counter, Default Translation Strategy

Re-verifies ["End-to-End Comparison: One Counter, Default Translation Strategy"](../../open-telemetry/opentelemetry-go/Preserving%20Prometheus%20Job%20and%20Instance%20in%20OTLP%20Translation.md#end-to-end-comparison-one-counter-default-translation-strategy) against a real running system, using the custom `otelcol-custom` collector built from the [`krajorama/opentelemetry-collector-contrib` branch `krajo/job-instance-persistence`](https://github.com/krajorama/opentelemetry-collector-contrib/tree/krajo/job-instance-persistence) (see [otelcollector/](otelcollector/)), rebuilt against commit `78a1b6d` ("Add POC option C1"). That branch implements Options A/B/C/C1 as four mutually-exclusive alpha feature gates, off by default:

- Receiver: `receiver.prometheusreceiver.jobInstanceOption{A,B,C,C1}`
- Exporter: `exporter.prometheusremotewrite.jobInstanceOption{A,B,C,C1}`

Only **B, C, and C1** are shown below — Option A is omitted (four columns made the tables too wide, and its bare `job`/`instance` resource attributes aren't a shape that makes sense in the OTel data model to begin with), and so is "Today" (no gates), since B/C/C1 already show the no-gates baseline implicitly wherever a column doesn't diverge from it. Option A's own results are unaffected by this update and remain available in git history.

**Option C1 is new**: as of commit `78a1b6d`, Option C's receiver-side (`AddTargetInfo`) gained "covered-name recognition" — it now recognizes `target_info`'s flattened `service_name`/`service_namespace`/`service_instance_id` labels as declaring the same identity as their dotted spelling, not just the literal dotted form. Option C1 is Option C *without* that recognition — i.e., exactly the old Option C behavior this report previously documented. The two share identical exporter-side (OTLP → Prometheus) logic; they differ only in what the receiver populates.

**Setup**: `oteljob` app — resource `service.name="my_service"`, `service.instance.id="my_id"`; meter `foo`; `Float64Counter("received_samples")` with attribute `a.a="b"`; default SDK translation strategy (no `-no-translation`). Scraped by the collector as `job_name="my_job"`, target `oteljob:9464`. Egress via the collector's `prometheus_remote_write` exporter to the custom-built Prometheus (`prometheus/`, branch `krajo/debug-otlp-rw-endpoint`). All three columns were actually run in the compose stack (`OTELCOL_FEATURE_GATES=<gate names> docker compose --profile standalone up -d --force-recreate oteljob otel-collector prometheus`), with Prometheus's storage wiped between captures; nothing here is inferred from source alone.

## Results

| Step | Option B — namespaced | Option C — declared-first | Option C1 — declared-first, no covered-name recognition |
| :--- | :--- | :--- | :--- |
| **1. OTel metric data model** (in-process, SDK; option-independent) | `Resource{service.name="my_service", service.instance.id="my_id"}`<br>`Scope{name="foo", version=""}`<br>`Metric{name="received_samples", type=Sum, monotonic=true, temporality=cumulative}`<br>`DataPoint{attributes={"a.a":"b"}, value=1}` | Identical to Option B | Identical to Option B |
| **2. SDK Prometheus exporter exposition** (curled `oteljob:9464/metrics` directly; option-independent) | `# HELP received_samples_total`<br>`# TYPE received_samples_total counter`<br>`received_samples_total{a_a="b",otel_scope_name="foo",otel_scope_schema_url="",otel_scope_version=""} 1`<br>`# HELP target_info Target metadata`<br>`# TYPE target_info gauge`<br>`target_info{service_instance_id="my_id",service_name="my_service",telemetry_sdk_language="go",telemetry_sdk_name="opentelemetry",telemetry_sdk_version="1.45.0"} 1` | Identical to Option B | Identical to Option B |
| **3. Collector `prometheusreceiver` → OTLP Resource** (debug exporter, verbosity `detailed`) | `Resource{`**`prometheus.job="my_job"`**`, `**`prometheus.instance="oteljob:9464"`**`, service.name="my_job", service.instance.id="oteljob:9464", server.address="oteljob", server.port="9464", url.scheme="http", `**`service_instance_id="my_id", service_name="my_service"`**`, telemetry_sdk_language="go", telemetry_sdk_name="opentelemetry", telemetry_sdk_version="1.45.0"}` | `Resource{prometheus.job="my_job", prometheus.instance="oteljob:9464", server.address="oteljob", server.port="9464", url.scheme="http", `**`service.name="my_service"`**`, `**`service.instance.id="my_id"`**`, telemetry_sdk_language="go", telemetry_sdk_name="opentelemetry", telemetry_sdk_version="1.45.0"}` — **`service.name`/`service.instance.id` are populated from the flattened `target_info` labels**, and there are no separate `service_name`/`service_instance_id` duplicates.[^1] | `Resource{prometheus.job="my_job", prometheus.instance="oteljob:9464", server.address="oteljob", server.port="9464", url.scheme="http", `**`service_instance_id="my_id", service_name="my_service"`**`, telemetry_sdk_language="go", telemetry_sdk_name="opentelemetry", telemetry_sdk_version="1.45.0"}` — same shape as Option B minus the job-derived `service.name`/`service.instance.id`.[^1] |
| — `Metric`/`DataPoint` (same block, all three columns) | `Metric{name="received_samples_total", type=Sum, monotonic=true, temporality=cumulative, metadata={prometheus.type="counter"}}`<br>`DataPoint{attributes={"a_a":"b"}, value=1}` | Identical to Option B | Identical to Option B |
| **4. `prometheus_remote_write` exporter → Prometheus series** (queried Prometheus, `{path="remote_write"}`) | `received_samples_total{job="my_job", instance="oteljob:9464", a_a="b", otel_scope_name="foo"} 1`<br>`target_info{job="my_job", instance="oteljob:9464", service_name="my_service", service_instance_id="my_id", server_address="oteljob", server_port="9464", url_scheme="http", telemetry_sdk_*} 1` | `received_samples_total{`**`job="my_service", instance="my_id"`**`, a_a="b", otel_scope_name="foo"} 1`<br>`target_info{`**`job="my_service", instance="my_id"`**`, `**`prometheus_job="my_job", prometheus_instance="oteljob:9464"`**`, server_address="oteljob", server_port="9464", url_scheme="http", telemetry_sdk_*} 1` — **the app's own declared identity now wins**, and the scrape pair survives as ordinary `target_info` metadata.[^2] | **Byte-identical to Option B** on both series — this is exactly what the report previously called "Option C."[^3] |
| **Net effect** | Scrape identity (`job`/`instance`) wins on every series; the app's declared identity survives only as `target_info` metadata. | **This is the first configuration in this whole set of reports that achieves true declared-first behavior under the default translation strategy.** `received_samples_total` is keyed by the app's own `service.name`/`service.instance.id`, and the scrape's `job`/`instance` are demoted to queryable `target_info` metadata — the reverse of every other column. | Reproduces the gap the original report flagged: the declared-identity check never fires because `service.name`/`service.instance.id` never arrive under that spelling, so the output is indistinguishable from Option B. |

## Footnotes

[^1]: Both C and C1 share the same receiver-side "never-derive": `CreateResource` (`prom_to_otlp.go`) sets *only* `prometheus.job`/`prometheus.instance` from the scrape and leaves `service.name`/`service.instance.id` unset, regardless of which gate is on. The two options diverge only in `AddTargetInfo` (`transaction.go`): C1 copies every `target_info` label verbatim (the original, unmodified behavior), so the app's declaration lands under whatever spelling it was exposed with — here, flattened `service_name`/`service_instance_id`, since the SDK's default translation strategy escapes the dots before the scrape ever happens. C instead runs `addTargetInfoWithCoveredNameRecognition`, which recognizes `service_name`/`service_namespace`/`service_instance_id` as aliases of the dotted `service.name`/`service.namespace`/`service.instance.id` and writes the canonical dotted key (collapsing agreeing spellings, dropping disagreeing ones rather than guessing) — so the same input that leaves C1's Resource with flattened keys leaves C's Resource with dotted ones.

[^2]: The exporter-side logic is *unchanged* from what previously ran as "Option C": `setJobAndInstanceLabels`'s declared-identity branch simply checks whether `service.name`/`service.instance.id` (dotted) are present on the Resource, and derives `job`/`instance` from them if so. What changed is that the receiver now actually populates those dotted keys under the default translation strategy (see [^1]), so this check — which always existed — finally has something to find.

[^3]: This column is exactly what commit `78a1b6d`'s message describes: Option C's old behavior, preserved as its own gate. Compare directly against the original report content (still in git history) for Option C before this commit — the results are identical, because C1 *is* that old behavior, unchanged.
